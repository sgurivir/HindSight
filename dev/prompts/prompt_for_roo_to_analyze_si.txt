
See report produced by a static code analyzer at ~/Desktop/issues.txt

Analyze if the issues are legitimate and worth fixing. Create a text output file of the issues which pass those two criteria. Output in that text file should match the format in the input file provided. 


Go through one issue at a time and check if issue is valid and worth spending time to fix it. If claim is partially correct or the impact is overstated, drop the issue.

This may be a long running task and might fail in the middle. Create a progress-<date>.md to mark your progress. You can use the markdown file to pickup where you left off if analysis fails in the middle.

Do not modify any metadata from original report. If a issue is found to be legitimate, copy over all metadata for the issue as-is.

CRITICAL REMINDER: Update progress.txt as frequently as possible, as you make progress
